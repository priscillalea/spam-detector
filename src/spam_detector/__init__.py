"""
spam_detector — NLP text classification package.

Modules:
    config       — centralised paths and constants
    preprocessing — text cleaning and normalisation
    train        — training pipeline and model comparison
    evaluate     — metrics, plots and model selection report
    predict      — inference class (loads model once)
"""
